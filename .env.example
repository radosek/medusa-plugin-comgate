# Comgate shop-link credentials (Client Portal → Integrace → Nastavení obchodů → Propojení obchodu)
COMGATE_MERCHANT=
COMGATE_SECRET=
# test mode: "true" = sandbox, anything else = PRODUCTION (real money)
COMGATE_TEST=true
